# TARJETA DE EVALUACIÓN

## Samir Ochoa

| Campo | Información |
|--------|-------------|
| **Evaluador** | Flavio Silva |
| **Evaluado** | Samir Ochoa |
| **Escenario elegido** | N.° 1|
| **Resumen de la respuesta** |Está mal porque a lo largo del día habrá hecho **muchos cambios** y usar un solo `commit` no es lo ideal, lo mejor es usar varios `commits` para identificar cambios en específicos y sea más claro que solo poner _"cambios"_.|
| **Qué entendió bien (una frase)** |Entendió que los `commits` deben hacerse con frecuencia para registrar cambios específicos y mantener un historial organizado.|
| **Qué necesita repasar (una frase)** |Debe reforzar la importancia de escribir mensajes de `commit` descriptivos y cómo los `commits` pequeños **facilitan encontrar errores o volver a versiones anteriores**. |

--- 

## Flavio Silva

| Campo | Información |
|--------|-------------|
| **Evaluador** | Samir Ochoa |
| **Evaluado** | Flavio Silva |
| **Escenario elegido** | N.° 2 |
| **Resumen de la respuesta** |Primero necesita la `URL` del repositorio para poder **clonarlo**, luego usar el comando `git clone <URL>` o si desea por `SSH` sería el comando `git clone <SSH>` y con eso basta para tener el repositorio en su máquina, luego ya puede trabajar y tener una rama para el o según le asignen.|
| **Qué entendió bien (una frase)** |Explicó correctamente el comando `git clone`, el uso de la `URL` del repositorio y que también puede `clonarse mediante SSH`. |
| **Qué necesita repasar (una frase)** |Debe reforzar que `git clone` también descarga todo el historial de `commits` y configura automáticamente el `remoto origin`, por lo que no es necesario ejecutar `git remote add origin`.|

--- 