# TARJETA DE EVALUACIÓN

## Samir Ochoa

| Campo | Información |
|--------|-------------|
| **Evaluador** | Flavio Silva |
| **Evaluado** | Samir Ochoa |
| **Escenario elegido** | N.° 1|
| **Resumen de la respuesta** |Devuelve `False` porque el `plato.stock` no es mayor a 0, para que sea `true` debería de ser mayor a 0.|
| **Qué entendió bien (una frase)** |Entendió la función `estarDisponible` y sus retornos.|
| **Qué necesita repasar (una frase)** |Cons respecto a esta función, nada, pues lo entendió perfectamente.|

--- 

## Flavio Silva

| Campo | Información |
|--------|-------------|
| **Evaluador** | Samir Ochoa |
| **Evaluado** | Flavio Silva |
| **Escenario elegido** | N.° 2 |
| **Resumen de la respuesta** |Saldría `Error` pues una `const` no se puede modificar, una **constante** se inicializa una vez se declara y no puede ser reasignada.|
| **Qué entendió bien (una frase)** |Entendió que `const` no se puede modificar.|
| **Qué necesita repasar (una frase)** |Explicar con mayor precisión por qué ocurre el error al intentar reasignar una `const`.|

--- 