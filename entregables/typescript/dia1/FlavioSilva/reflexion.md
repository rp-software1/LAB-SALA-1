# Reflexión - Flavio Silva 

## Preguntas 
1) ¿Qué concepto de `TypeScript` te costó más entender hoy?

    - Con respecto a `TypeScript` lo más difícil para mí fue usar el tipado, lo vi en `Java` pero así usarlo en `JavaScript` fue un poco confuso.

2) ¿Qué ventaja concreta viste de `TypeScript` vs `JavaScript` puro?

    - Pues la principal es el tipado, el cual ayuda durante el desarrollo, porque al ejecutar en el navegador muestra solo `JavaScript`, pero en el desarrollo es donde se ve la diferencia.

3) Total de errores `TypeScript` al cierre: 97 (lo bajaremos mañana)
